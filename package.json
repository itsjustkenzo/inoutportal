{
  "name": "inout-portal",
  "version": "1.0.0",
  "private": true,
  "description": "IN/OUT Portal - MERN stack employee in/out board",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "render-build": "npm run install:all && npm --prefix client run build",
    "demo:add": "npm --prefix server run demo:add",
    "demo:clear": "npm --prefix server run demo:clear",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
